# Module manifest. Parsed (never executed) by the setup wizard to build the
# module menu and drive setup. See docs/setup-wizard.md for the contract.

TITLE=Calendar
DESCRIPTION=Pull calendar events into the wiki over CalDAV (read-only).

# Command (argv, relative to this module directory) the wizard runs to verify
# a finished setup with a live connection.
CHECK=ingest.py check

# Minimum Python version.
REQUIRES_PYTHON=3.9

# Third-party import names to probe before setup (see requirements.txt).
PY_IMPORTS=caldav,icalendar
