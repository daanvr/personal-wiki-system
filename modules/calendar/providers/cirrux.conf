# Cirrux provider profile.
#
# Connection details ONLY. No passwords, no personal data — this file is
# committed and public. The account address and password live elsewhere
# (see ../config.example and SECRET_REF).
#
# Verified publicly on 2026-06-19:
#   https://api.cirrux.co/.well-known/caldav redirects to /caldav/
#   /caldav/ responds with Basic realm="CalDAV" when unauthenticated.

CALDAV_URL=https://api.cirrux.co/caldav/
AUTH=password
