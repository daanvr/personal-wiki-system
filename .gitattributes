# Normalize line endings: stored as LF in the repo, native on checkout.
* text=auto

# Shell scripts must stay LF even on Windows, or they fail to run on
# macOS/Linux with a "bad interpreter" error.
*.sh text eol=lf
